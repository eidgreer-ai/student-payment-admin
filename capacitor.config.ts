import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studentpayment.admin',
  appName: 'Student Payment Admin',
  webDir: 'dist/public'
};

export default config;
